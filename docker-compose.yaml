services:
  wallence:
    image: docker.io/kvtodev/wallence
    pull_policy: never
    container_name: wallence
    restart: unless-stopped
    ports:
      - "7890:7890"
    environment:
      - DIRECT_DOMAINS=${DIRECT_DOMAINS}
      - SUBSCRIPTIONS=${SUBSCRIPTIONS} 
